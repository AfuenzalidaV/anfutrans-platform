/**
 * Modelo TipoDocumento
 * Generado automáticamente desde Prisma Schema
 */
export interface TipoDocumento {
  id: number;
  codigo: string;
  nombre: string;
  ambito: string;
  activo: boolean;
}
