/**
 * Modelo CargoDirigencial
 * Generado automáticamente desde Prisma Schema
 */
export interface CargoDirigencial {
  id: number;
  codigo: string;
  nombre: string;
  nivel: string;
  activo: boolean;
}
